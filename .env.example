# Environment variables (copy to .env — never commit)

# STT (pick one provider in settings; keys here for local dev scripts)
# OPENAI_API_KEY=
# DEEPGRAM_API_KEY=
# HF_TOKEN=

# Local inference
# OLLAMA_HOST=http://127.0.0.1:11434
