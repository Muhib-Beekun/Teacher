# Environment variables (copy to .env — never commit)

# Grok / xAI (recommended for compile + STT polish — fast remote inference)
XAI_API_KEY=
# GROK_API_KEY=   # alias also accepted

# STT (optional)
# DEEPGRAM_API_KEY=
# OPENAI_API_KEY=

# Local inference (fallback when no Grok key)
# OLLAMA_HOST=http://127.0.0.1:11434
